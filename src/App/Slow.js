import React from "react";

const Slow = () => {
    /*function sleep (time) {
        return new Promise((resolve) => setTimeout(resolve, time));
      }
      
      sleep(500).then(() => {
        <div>Slow content</div>
      });
      */
  return (
    <div>Slow content</div>
  );
};

export default Slow;
