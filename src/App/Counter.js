import React from 'react';
import CounterWrapper from './Counter/CounterWrapper';

const Counter = (props) => {
    return (
        <div>
            <CounterWrapper />
        </div>
    );
}

export default Counter;